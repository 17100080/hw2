%h1 Delete Movie

= form_tag deleting_movies_path :method => :post do

  = label :movie, :title, 'Title'
  = text_field :movie, 'title'

  = label :movie, :rating, 'Rating'
  = select :movie, :rating, ['None', 'G','PG','PG-13','R','NC-17']

  = submit_tag 'Save Changes'